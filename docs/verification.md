# 验证记录

## 境界成长、炼器收益与秘境强度（2026-09-20）

- 大境界突破采用递增基础加成，小阶段维持原收益。天灵根、兼修、无额外根基功法时，合体后期→大乘初期伤害从 ×4.90 到 ×6.10（+24.49%），气血从 557 到 687（+23.34%）；大乘后期 / 待渡劫为 693 气血、×6.15，真仙为 893 气血、×12.30。成仙独立乘 2，不再把“+100%”加进已有伤害池。
- 炼器一至十阶累计加成为 8 / 16 / 24 / 32 / 40 / 52 / 68 / 88 / 114 / 150%。费用、等级与收藏保留；伤害、灵威与藏器阁共用同一收益函数。藏器阁增加下一阶实际提升，灵威明确只作同一本命下的入场成长参考。
- 第二至第六境按固定关卡表逐步增加气血与伤害，普通怪伤害增幅低于精英和妖王；第七境气血与伤害乘 1.35。怪物不读取角色境界或灵威，不随玩家提升追涨；数量、速度、预警、经验与修为奖励保留。已在场的旧怪保留快照属性，后续生成应用新表。天劫仍使用独立规则。
- 新增五项成长回归：先在旧版确认四项失败，再验证后期跨境收益、七档灵根 × 三路线 × 根基功法的真仙翻倍、实际命中与炼器倍率、存档及灵威一致性、怪物不随玩家动态增强。同步原属性断言，保留旧局缺血、死亡不复活、重复恢复不反复回血、碰撞 / 弹幕 / 地面技能继承伤害的断言。全量 **226 项测试通过**，TypeScript 与独立目录生产构建通过。
- 同一组固定种子、同一战斗引擎，修改前后分别运行六境连续模拟、终关及天劫模拟。初入仙途三路线 × 三种子 × 六境的 54 局前后均首次通关，新版第六境 423–437 秒、结束 LV75–83；第一境三档难度各三样本依次为 3/3、3/3、1/3 通关；另十二组本命 / 路线有 11/12 通关，失败样本与修改前相同（高难两组、魔道噬魂砂一组）。终关相同根基 10/10/8、五阶炼器、四个起始境界 × 三种子，旧版 2/12 通关，新版 3/12，通关 638–707 秒；新增 `npm run balance:trial -- --max-forge` 同组十阶炼器 7/12 通关，通关 612–671 秒。天劫满根基、十阶炼器、六件觉醒、允许一次复活：五轮各九种子，新版结果依次 9/9、9/9、8/9、9/9、0/9，与修改前相同。这些是有限的 AI 样本，过程中可升级 / 突破、随机选择会分叉，不能据此推算真人胜率或断言某个起始境界一定更容易。
- Chrome DevTools MCP 在唯一隔离测试页验证桌面 1440×1000、手机 390×844。洞府待渡劫显示基础气血 +593、基础伤害加成 +515%；真仙履历显示 893 气血、×12.30。实际从九阶炼到十阶，6000 灵石 / 400 玄铁变为 1060 / 70，显示 +150% 与满阶禁用，刷新后仍为十阶。桌面与手机截图无布局溢出，手机面板 clientWidth / scrollWidth 均为 358，文档宽度 390。

- 格式检查与 diff 空白检查通过。本机 5173 已原子更新为 `index-BfGQaIO8.js` / `index-DJiyFhDa.css`，旧脚本仍返回 200，服务 PID 16552 保持。Cloudflare Pages 生产部署 `9146166a-eb41-4791-b5d1-daf4ebef3c84`，正式域名实页确认同一脚本、真仙独立翻倍说明、九升十阶扣料与 +150% 收益，手机宽度无溢出，未见控制台 error / warn。主站冷加载及移动 / 桌面切换曾超过 MCP 导航等待时间，页面随后可交互；不把导航超时当作加载性能通过。未执行 Git commit / push，GitHub Pages 未同步。

## 暮色序章、仙途履历、热闹街市与真仙云海（2026-09-20）

- 保留亮版 `qinglan-prologue.webp`（249,902 字节，README 继续引用），内置 imagegen 编辑生成独立暗版 `qinglan-prologue-dark.webp`（1672 × 941，212,726 字节）。序章改用暗版和浅色正文；暗图的 PNG→WebP 压缩为 92.2%。首访只下载暗版；真仙首页另预载亮版。未触发铁匠故事不显示指路卡片，游戏指南也移除剧情剧透。
- 小镇新增 48 位横纵街行人与最多 12 位店前顾客，复用人物图集，不改变原 21 位具名镇民和姓名。顾客跟随迁城后的实际店铺，屏幕外隐藏；没有新增城镇图片、行人 AI 请求或碰撞阻挡。桌面初始视野新增人物约 18 位，手机约 12 位，其余隐藏。连续进出六次始终一张地图、60 个新增人物、350 个 DOM 元素，未见节点累积。
- Chrome DevTools MCP 同一隔离页、同种子、相同往返路段进行 10 秒前后对比：1440 × 1000 桌面前后均约 16.67 ms/帧、P95 16.7 ms、无超过 50 ms 的帧或长任务；390 × 844、4 倍 CPU 降速前后均约 16.7 ms/帧、P95 16.8 ms、无长任务。844 × 390、6 倍降速首轮新增版出现一次 57 ms 长任务；最终版本重测平均 16.67 ms、P95 16.8 ms、无长任务，旧版对照平均 16.69 ms、无长任务。桌面滚动 trace 的 CLS 为 0。这些是本机浏览器模拟、短时且资源已加载的样本，不代表低端真机或长时间游玩保证。
- 「仙途履历」保存此世真实年岁、逐阶段突破、首次通关、闭关、炼器、宗门和亲历旧事；最近 200 条年表与 21 项独立成就分开保存，轮回清空，导出导入保留。旧档未知年龄留白，存档文本经过转义。首页「灵威」复用实际一级满血入场属性，只在页面打开时计算，不修改存档和战斗数值，不因灵石或未装备的法宝上涨。
- 修为达标且第七境未通过时显示「渡劫」，数值仍为大乘后期；过关才成真仙并获得 +200 气血、+100% 伤害的突破加成。原已验证边界为累计修为 119,940，低于阈值仍为大乘后期。真仙才切换晨光云海、轻薄半透明首页；渡劫及之前保持原主题。文案按要求改为「在青岚镇长大」。
- 供奉不足停在人间账期边界，广告可分次补足，取消广告回到待缴界面，明确放弃才清退；刷新保留待缴状态。同刻到期的精研先等补缴，补缴完成才给成果。浏览器实际观看 5 秒：0 灵石领取 300 / 30 后补缴 80，剩余 220 灵石 / 30 玄铁、账期变 40 年、年岁维持 35；放弃路径身份清空，履历记录清退。
- 全量 221 项测试通过；TypeScript、独立目录生产构建、格式检查与 diff 空白检查通过。新增验证覆盖跨境/跨真仙、旧档未知年龄、记录上限与成就保留、导入与文本转义、灵威无副作用及成长一致性、欠费停时/分次补缴/同刻精研/重复扣费。未改战斗平衡，本轮不重复平衡模拟。
- Chrome DevTools MCP 桌面及手机截图已检查暗序章、真仙首页、履历弹窗和新城镇；无横向溢出。渡劫样本无仙境主题类，主页仍为原样式；真实年龄与真仙条件显示一致，控制台无 error / warn。只使用同一个隔离测试页，先前多余的 25 页均已清理，未操作玩家存档。

- 本机 5173 已原子切换到 `index-BcsufsiV.js` / `index-DJiyFhDa.css`，旧脚本仍返回 200，服务 PID 16552 未重启。Cloudflare Pages 生产部署 `0635a04f-07f9-4d27-be99-2e78ae331e72`，正式主站实页确认相同脚本、暗序章、真仙云海、履历年岁与隐藏故事入口；城镇 60 位新增人物加载完成，手机端实际完成广告领取与补缴，结果同本机，控制台无 error / warn。验收后唯一测试页已退回空白，停止测试游戏运行，临时入口已移除。线上首次加载部分素材约 7–10 秒、首次终关加载超过工具默认等待时间，随后资源全部就绪并正常显示；不是帧率测试，不据此承诺弱网速度。没有执行 Git commit / push，GitHub Pages 未同步本轮。

## 一次复活、三代炉火旧事与真仙境（2026-09-20）

- 广告复活改为每局 / 每次天劫最多一次。先用回归测试复现第二次仍可复活，再收紧上限；旧版已用 2–10 次的续局仍可读取，保留已用次数且不能再复活。寿尽借寿仍是独立机制。实页首死显示剩余一次，5 秒广告后满血并有三秒护体；合成第二次死亡续局直接结算，存档不再保留对局，没有广告入口。
- 新增《炉火未凉》三章固定故事：当代铁匠修钟、下一代旧信、再下一代赠铃。首次相识以实际年岁为起点，赠两枚玄铁或免费帮忙各有回忆，旧钟可留渡口或送学堂，相关镇民谈资随之变化。错过数代可依次补读旧信；纪念物与一次性玄铁由按钮发放，AI 无数值操作能力。故事保存到 `mortal.smithStory`，兼容旧档，导出导入保留、轮回清除；人间入口可重读。
- 末境统一为「真仙」，不分初中后期，突破奖励为气血 +200、法宝伤害 +100%（沿用境界加成叠加方式），前七次大境界仍为 +45 / +35%。保留渡劫战斗的过程名称及通关后不再降周期天劫。旧末境续局按新加成重算，真实页面合成样本 420/563→520/663，保留缺失的 143 气血；重复读档不额外回血，修为、收藏和通关记录不变。
- 全量 **215 项测试通过**，Worker 类型检查、独立目录生产构建、格式与 diff 空白检查通过。覆盖故事四条选择组合、换代边界、跨越数代、旧玩家开篇、导入坏数据、重复领奖、AI 故事上下文及失败，复活限制与旧档、真仙门槛及属性迁移。本轮没有改变成仙前的战斗数值，未重跑全部难度模拟。
- Chrome DevTools MCP 使用隔离合成存档检查桌面 1440×1000、手机 390×844。实际步行至铁匠，与于怀谷相识赠铁（8→6），80 岁见任立春并送钟学堂，140 岁见严向晚收铃（6→12）；重复点击和刷新后仍为 12，入口保留故事。手机故事及按钮可滚动触达、无横向溢出。离线发送新闲谈回退本章台词，输入恢复可用，故事仍能阅读。本验证包含测试年岁注入及手机模拟，不等同于真人历时体验或真机测试。
- NPC Worker 发布 `9c176057-8ebe-4176-b574-946f35007430`，加入已确认故事与前人书信的事实提示、禁止编造玩家姓氏。模型仍为 GLM-4.7-Flash，响应质量受模型影响；固定故事与选择不依赖模型输出。没有新增图片资源。

- 本机 5173 安全更新为 `index-DfZwXCIQ.js` / `index-Dfn_CANp.css`，保留旧 `index-g3oVwZJv.js`（实页请求 200），服务仍为 PID 16552，未重启或刷新玩家页面。Cloudflare Pages 生产部署 `0105b8a1-c136-4c2a-9cfc-b7a5a363f720`，正式主站确认相同脚本、真仙首页、走到铁匠后可免费帮忙并保存选择，真实 GLM 对话返回 200，脚本、样式及城镇素材返回 200 / 304，控制台无 error / warn。临时验证入口已清理。本轮未执行 Git commit / push，GitHub Pages 未同步。

## GLM 对话与百年城景变迁（2026-09-20）

- 默认模型按用户指定切为 `@cf/zai-org/glm-4.7-flash`。当前账号实际调用 GLM-5.3-Flash 返回套餐限制；GLM-4.7-Flash 可用，使用关闭深度思考的短对白模式、512 tokens 上限，保留 12 / 14 秒前后端超时及原对白兜底。新增城景背景与接续话题的提示；旧客户端没有城景字段也可正常交谈，只返回最终文本，不展示模型推理。
- `mortal.scenery` 保存上次入城年岁和城景版本。实际入城时相隔满 100 年才换景；短别不换，千年离别一次只生成一个版本，买卖重绘与素材重试不记为新访问。旧档第一次载入保留旧城，种子和版本随存档及导入导出保留。八处沿街营业场所随房舍迁位，三处住宅成为树木空院，陈设位置和大小变化；河道、码头、道路不动，21 位镇民、近邻判定和小地图共用新坐标。
- 全量 206 项测试通过；最终切换 GLM 后，19 项城景 / 人口 / 对话测试再次通过。Worker 类型检查、TypeScript 和独立目录生产构建通过。覆盖 100 年阈值、短别访问基准、久别、旧档与坏数据、确定性布局、历代店铺迁位、所有 NPC 的可达性和行走交谈、人物身份不受房屋迁位影响，以及新模型调用参数、旧请求兼容和只展示最终回答。本次不调整战斗或成长数值，未重跑平衡模拟。
- Chrome DevTools MCP 隔离档按 15→115 岁真实入城路径触发一次变迁：28 栋旧屋变为 25 栋房舍与三处空院；坊市由 `(2310,1250)` 迁至 `(2085,1848)`。实际走到新址交谈、购入玄铁，灵石 300→270、玄铁 0→1，弹窗保留，上次入城年岁和版本均不变。立即离城再入、刷新后重入仍保持相同布局。
- 桌面 1440 × 1000 对照截图确认房舍与商铺位置变化；手机 390 × 844 检查新城景、状态栏、空院、移动和操作按钮，无横向溢出，控制台无 error / warn。复用现有图集，没有新增图片下载。上述为桌面浏览器及手机模拟验证，不代表手机真机表现。
- 已部署 NPC Worker 版本 `f9941f7a-ee22-4acc-a48d-d2fc4e31efbd`；自定义域名健康检查显示 GLM-4.7-Flash，浏览器真实对话成功，单条样本约 1.2 秒。该响应时长仅为当前样本，不是延迟保证或模型质量评测。
- 本机 5173 安全更新为 `index-g3oVwZJv.js` / `index-Cjg2fKEx.css`，旧 `index-qT9o6aou.js` 保留且返回 200；未重启服务或刷新玩家页面，临时验证入口已清理。Cloudflare 主站部署 `fc5fbbbb`，正式域名隔离页面再次验证百年入城为版本 1、25 栋房舍与三处空院、21 位 NPC、GLM 健康检查与对白；脚本及场景资源返回 200 / 304，对话返回 200，控制台无 error / warn。格式和 diff 空白检查通过。本轮未执行 commit / push，GitHub Pages 未同步。

## 青岚镇民换代、AI 闲谈与少年身世（2026-09-20）

- 九位职业镇民与十二位沿街村民共用中文姓氏 / 名字词库和存档种子，同镇不重名，刷新与导入导出不改名。按角色总年岁每 50–70 年换为新人物，历练、闭关也计入；换代更新姓名、配色与对话身份，保留营生。直接定位当代人物，长年岁不逐代循环；复用九人 WebP 图集，没有新增图片负担。
- 序章与首页加入主角生于青岚镇、十五岁离乡的经历，明确觅长生、追寻大道的志向。Worker 的人物背景同步，凡人没有前任私人记忆。默认调用 `qinglan-npc-ai.011203.xyz`，使用 Workers AI 的 `@cf/qwen/qwen3-30b-a3b-fp8`；域名、模型、超时、长度与换代常量集中在 `src/setting.ts`，部署绑定与来源白名单在 Worker 配置中。
- 全量 201 项测试通过；随后修复买卖后立即重开弹窗时、场景尚未完成首帧导致邻近人物判断为空的问题，14 项 NPC / 城镇测试及独立目录生产构建再次通过。Worker 类型检查、格式与 diff 空白检查通过。覆盖稳定姓名、50–70 年边界、小数年岁、极长年岁、旧档与导入、全局计龄、行人可达性、CORS、请求校验、限流、AI 失败、超时与取消。本次 NPC 改动不调整战斗数值，未重复运行平衡模拟。
- Chrome DevTools MCP 隔离合成存档验证桌面 1440 × 1000、手机 390 × 844 的序章、首页、城镇与交谈。21 位人物及姓名完整，手机无横向溢出。真实 Worker 对村民的招呼及“离乡求长生”消息返回 200；同页跨越 78 年换代节点，蒋守拙变为郝仲平，新请求历史为空，不携带前任的蓝伞对话。刷新后姓名与代际保持一致。
- MCP 离线模拟下，交谈回退原台词并恢复输入；坊市买入后灵石 270→240、玄铁 1→2，弹窗保持，AI 不可用不影响买卖。用页面请求桩返回 HTML 字符串时仅展示文字，没有生成 img 或执行事件；关闭交谈会中止请求，迟到回复不重新打开弹窗。这些是隔离页面和浏览器模拟验证，不代表所有手机或真实弱网情形。
- NPC Worker 已发布版本 `03476ef1-d9ce-4458-85c5-1f8c472692ed`，自定义域名健康检查与真实对话可用。补充对白只使用中文的提示约束，Worker 三项回归与类型检查再次通过。聊天仅保留当前页面内存，不上传完整存档，不接入数值修改工具；README 保持简短，玩法指南、开发说明与协作指南同步。
- 已安全更新本机 5173（`index-qT9o6aou.js` / `index-DTTQk-z6.css`），旧 `index-Bf1EAHFh.js` 仍返回 200；不重启服务、不刷新玩家页面，临时入口已移除。Cloudflare Pages 生产部署为 `3bdedbb0-6bf2-4d60-8376-cdf18b80f7e7`，正式主站实页确认新序章、首页身世、21 位镇民与真实 AI 对话，脚本、样式、场景 WebP 及对话请求均返回 200，控制台无 error / warn。本次连同前六境 335577 节奏一并发布；未执行 Git commit / push，GitHub Pages 尚未同步本轮改动。

## 前六境 335577 分钟节奏（2026-09-20）

- 前六境倒计时改为 3 / 3 / 5 / 5 / 7 / 7 分钟，合计 30 分钟，妖王战另计。四批妖物继续按 0% / 25% / 50% / 75% 登场，精英间隔随时长缩短；第七境十分钟、七王登场时序、升级经验曲线和奖励保持不变。游戏内指南与玩法文档同步。
- 上一版续局按快照记录的原时长换算完成比例；更早没有时长字段的快照按原始 5–10 分钟迁移。重复读档不重复压缩，保留已出场妖王及其血量、掉落、已获奖励和已消耗年岁。调整妖物隔离测试的固定采样点为关卡 25% / 50% / 75%，避免在新妖王登场时刻误把妖王当作普通精英。
- 全量 190 项测试、TypeScript 检查、独立目录生产构建、Prettier 与 diff 空白检查通过。固定种子平衡模拟完成；正道 / 魔道 / 兼修各三组连续六境样本均通关，第六境结束为 76–83 级，没有提前达到 100 级。这是自动模拟样本，不代表真人通关率或所有存档的成长速度。
- Chrome DevTools MCP 隔离存档检查桌面 1440 × 1000、手机 390 × 844，七张关卡卡片显示 3 / 3 / 5 / 5 / 7 / 7 / 10 分钟，无横向溢出。上一版第二境 120 / 240 秒样本恢复为 90 / 180 秒，HUD 显示 01:30 / 03:00，10 级与 88 / 100 气血保持；再次载入仍为 01:30。指南展示正确新时长，控制台无 error / warn。
- 已安全更新本机 5173（`index-Bf1EAHFh.js`）；正式入口再次确认新版卡片与指南，旧 `index-DT2-ROqn.js` 仍返回 200。保留旧资源、不重启服务、不刷新玩家页面，临时入口已移除。本轮未提交、推送或部署远端。

## README 精简与 Cloudflare 主站同步（2026-09-20）

- README 从 234 行缩至 31 行，保留封面、主入口、四项特色和简短启动命令。详细规则迁入 `docs/gameplay.md`，开发与部署迁入 `docs/development.md`；相对链接和封面路径检查通过，格式及 diff 空白检查通过。本次仅调整文档，不改游戏逻辑，未重复运行本地战斗测试。
- README、GitHub 仓库网站栏、开发指南与 AGENTS.md 均明确 Cloudflare 为主站、GitHub Pages 为备用。主站地址为 `https://qinglan-xiuxian.pages.dev/`。
- 通过项目发布命令完成类型检查、独立目录构建和 Cloudflare 上传，部署地址 `https://15490a51.qinglan-xiuxian.pages.dev`。发布的是已验证的 `a920e9c` 游戏代码，未提交部分仅为此次文档整理。
- Chrome DevTools MCP 隔离页面确认 Cloudflare 正式域名加载 `index-DT2-ROqn.js`，序章与首境九张 WebP 均返回 200；进入首页、开局与暂停正常，控制台无 error / warn。没有改动或刷新玩家页面，5173 服务和产物保持不变。

## GitHub 发布前检查与仓库信息（2026-09-20）

- 汇总城镇扩建、压缩素材与预载、成长属性调整、晨曦背景及序章的当前版本；重新运行 189 项测试、TypeScript 检查、GitHub Pages 子路径生产构建、Prettier 和 diff 空白检查，全部通过。独立构建目录为 `/tmp/qinglan-github-release`，未覆盖正在服务的 5173。
- 仓库简介更新为当前 36 种法宝、16 部功法、七重秘境、五行灵根与人间宗门，网站栏填写 GitHub Pages 正式地址。新增 xianxia、browser-game、survivors、roguelite、typescript、vite、canvas、single-player 八个主题；保留公开状态。README 顶部增加内容概览、在线试玩及构建状态链接。

## 晨曦山水背景与「山河一梦」序章（2026-09-20）

- 内置 imagegen 生成背景后，根据反馈将夜色重绘为明亮晨曦、青绿群峰、云海和仙鹤；最终 WebP 1672 × 941，249,902 字节，较 PNG 减少 91.2%。序章与 README 共用最终图片，浅色衬底和墨绿正文保留晨光色调。
- 首次进入在场景预载中补齐背景后才展示序章。已读状态保存在 `prologueSeen`，进入或略过均记为已读；刷新、轮回和导入导出保留该状态。老档缺失字段时可读一次，寿尽 / 待渡劫提示优先。背景不加入已读玩家的日常加载。
- 全量 189 项测试通过，包括新增的已读状态保存、缺失字段迁移与导入导出验证；非法非布尔字段被拒绝。类型检查、独立目录生产构建、Prettier 与 diff 空白检查通过。本次未改变战斗数值，未重跑平衡模拟。
- Chrome DevTools MCP 隔离存档检查桌面 1440 × 1000、手机竖屏 390 × 844 和横屏 844 × 390，正文可滚动到末尾、两个入口按钮完整可见、无横向溢出。修正浅色标题对比度，并去掉全屏序章淡入时透出首页的闪烁。控制台无 error / warn。
- 实页验证入此山河、略过、Esc 均关闭序章并保存；刷新不再弹出，背景资源请求数为 0。旧档样本的年岁 15、灵石 567、修为 1234 在略过后保持不变。首页在序章期间设为 inert，关闭后恢复。寿尽样本优先显示借寿 / 轮回提示。
- 通过 Image.src 单次失败桩模拟背景加载失败，序章保持关闭、未写入已读状态，重试后完整展示。这是加载失败分支验证，不等同于真实弱网或手机真机测试。
- 已安全更新 5173（`index-DT2-ROqn.js` / `index-Bp-1EXuc.css`），正式入口的隔离页面确认新版序章和 249,902 字节背景；旧 `index-CuyDHQXk.js` 仍返回 200。临时入口已移除，未刷新玩家页面。本轮未提交、推送或部署远端。

## 灵根基础暴击与移速分档（2026-09-20）

- 天灵根、异灵根基础暴击率 7% / 移速 175；普通双、三系 5% / 160；伪四、五系 3% / 150；无灵根 1% / 140。移速单位为地图像素 / 秒。身法、功法、闭关与精研加成规则不变，暴击率上限仍为 85%，基础暴击伤害仍为 1.8 倍。只改变历练基础属性，城镇行走不变。
- 属性读取开局锁定的灵根，洗根不会改动未结束的旧局；载入旧局按该局资质应用新基础值，更早无资质字段的存档继续按天灵根兼容。首页、资质指南及广告自选灵根同步展示基础暴击和移速。
- 2 项新增回归用例先在旧实现下失败，修改后全量 188 项测试通过。验证七档基础值、真实移动距离、身法 / 闭关 / 功法叠加、暴击封顶、洗根后的旧局 / 新局及旧快照兼容。类型检查、独立目录生产构建、Prettier、diff 空白检查通过。
- Chrome DevTools MCP 隔离存档实测七档自选灵根，分别显示 7/7/5/5/3/3/1% 与 175/175/160/160/150/150/140；领取五系伪灵根后首页和本机存档同步为伪灵根，展示 3% / 150。桌面 1440 × 1000、手机 390 × 844 截图确认首页和广告选择面板正常，无横向溢出、控制台 error / warn。未把属性回归测试当作长期通关率验证。

- 已安全更新本机 5173（`index-CuyDHQXk.js`）；正式入口确认伪灵根 3% / 150，旧 `index-CgSJkwRS.js` 仍返回 200。临时验证入口已移除，未刷新玩家页面，未提交、推送或部署远端。

## 城镇全屏与白骨百分比成长（2026-09-20）

- 青岚镇改为独立铺满视口的场景，年岁 / 物资 / 研习状态、离城和交谈操作悬浮在地图上；人间页签、长介绍与宗门页面在离城后恢复。地图镜头随窗口变化更新并清空移动输入，城镇位置在本次页面内保留。
- 手机点击入城时共用历练的 MobileDisplay，在点击调用栈中请求全屏，之后尝试横屏；权限失败仍正常显示地图。对话、物资不足与广告保持在同一全屏根节点内；离城释放本次全屏和方向锁并保存年岁，未加载、弹窗、后台等计时门禁不变。
- 白骨魔功每重气血 +14%，骨刺每重基础伤害为最大气血的 12%，继续受伤害倍率及宗门精研增强。旧局沿用原有血量迁移，保留损失气血，重复读档不补血、死者不复活。
- 3 项白骨回归测试先在旧代码下失败，改后通过；全量 186 项测试通过。覆盖不同境界、五重叠加、正道 / 劫印 / 闭关 / 金刚协同、骨刺伤害统计、受伤无敌和致命伤门禁、旧局迁移；已有十项全屏生命周期用例保持通过。类型检查、独立目录生产构建、格式与 diff 空白检查通过。
- Chrome DevTools MCP 隔离存档实测桌面 1440 × 1000、手机模拟竖屏 390 × 844、横屏 844 × 390：地图边界与整个视口一致，无页面滚动，HUD、交谈和离城按钮可见。修正手机全屏按钮换行后重新截图确认。键盘沿街移动 468 地图像素，靠近坊市打开交易；弹窗前后年岁不变，全屏内物资不足提示正常。手机真实点击入镇成功进入浏览器全屏，当前模拟器保持竖屏；离城后 fullscreenElement 为空，入口恢复并暂停计龄。未用真机验证系统横屏许可。
- 浏览器合成渡劫三重白骨 / 十阶精研旧局：原气血 681 / 718，续局后为 988 / 1025，仍损失 37 气血；宗门卡片同步展示两个百分比。测试页面控制台无 error / warn，未操作玩家页面或存档。

- 已安全更新本机 5173（`index-CgSJkwRS.js` / `index-1OJQ1glG.css`）；正式入口隔离页面确认新脚本和完整全屏城镇，旧 `index-BBePhe0Z.js` 仍返回 200。临时验证入口已移除，未提交、推送或部署远端。

## 精研境界门槛与十五岁起步（2026-09-20）

- 十阶精研依次要求炼气后期、筑基初期、金丹初期、元婴初期、化神初期、炼虚初期、合体初期、大乘初期、大乘后期、渡劫。境界只作门槛，不消耗修为；未达标不扣费，第七境未通关不能解锁十阶。
- 旧档已学精研保留，实际效果按当前境界上限计算；超门槛的进行中研习保留费用与剩余时间，突破后入城继续。新角色、随机灵根与轮回从 15 岁开始，已有明确年龄保留，缺失年龄字段的旧档初始化为 15 岁。
- 5 项新增回归用例验证十阶临界值、未通关的超额修为、旧档及导入的实际战斗加成、旧研习暂停与恢复、年龄兼容。全量 183 项测试通过；更新寿元与闭关边界测试，确认炼气未借寿从 15 岁在第一境累计战斗 8.5 分钟寿尽。类型检查、独立目录生产构建、Prettier 和 diff 空白检查通过。
- Chrome DevTools MCP 隔离存档实测：炼气初期按钮为「需炼气后期解锁」，即使无灵石点击也不会出现物资广告或扣费；修为 205 达标，点击研习仅扣 30 灵石、保留修为，宗门页不推进年岁或研习。旧十阶低境界显示已学 10 阶 / 当前生效 0 阶；大乘九阶锁定十阶，通关渡劫后开放 210 灵石研习。旧二阶研习显示暂停及筑基门槛，剩余 0.5 年保留。
- 桌面 1440 × 1000、手机模拟 390 × 844 截图检查精研信息与按钮，无横向溢出。新档显示 15 岁；旧 50 / 100 岁存档不变，实际点击确认轮回后存档、首页均为 15 岁，宗门成果清空。页面控制台无 error / warn；未操作玩家真实页面和存档。

- 已安全更新本机 5173（`index-BBePhe0Z.js`）；正式入口隔离页面确认新脚本及轮回后的 15 岁，旧 `index-Ce7D9ckR.js` 保留。临时验证入口已移除，未提交、推送或发布远端。

## 宗门供奉单次封顶（2026-09-20）

- 单次供奉上限为 1000 灵石：炼气 / 筑基 / 金丹仍为 80 / 200 / 450，元婴至大乘均为 1000。所有缴费间隔不变，大乘仍为 5000 年，渡劫仍免供奉；入门礼和精研费用不变。
- 本机读档与文件导入会把未缴超额账单降至上限，保持原到期日、灵石余额、成员身份和研习 / 精研成果；结算也按封顶金额检查余额。已缴费用不追溯。
- 3 项新增用例先在旧实现下失败，修改后全量 178 项测试通过，类型检查、独立目录生产构建、格式和 diff 空白检查通过。覆盖九大境界、新入宗、旧档和导入迁移、余额 999 / 1000 / 1200 的扣款与清退边界。
- Chrome DevTools MCP 隔离存档实测：30000 灵石旧账单加载后为 1000，到期日仍为人间 5000 年、精研三阶保留。余额正好 1000 时入城到期扣为 0，保持太玄剑宗身份，下次到期为 10000 年，见闻记载实际缴费 1000。
- 桌面与 390 × 844 手机截图确认收费表、封顶说明及精研按钮正常，手机无横向溢出。未改动精研机制：宗门页签点击按钮先付费，进入前台城镇后才推进研习。
- 已安全更新到 5173（`index-Ce7D9ckR.js`），正式入口隔离页面确认供奉封顶，旧 `index-4283NGvE.js` 保留。未刷新玩家页面、未提交或推送。

## 模块化人间城镇、素材减重与按需补给（2026-09-20）

- 人间入口分城镇 / 宗门页签。青岚镇为 3600 × 2500 模块地图，28 栋建筑、九位不同职业镇民、12 位复用图集的往来行人、菜摊、晾衣、炊烟、灯笼及三处港口栈桥；地图按坐标拼装，镜头跟随。九种 NPC、九种建筑和六种杂物经内置 imagegen 生成并验证透明通道；未采用四人草案和整张城镇背景。
- 175 项测试通过，覆盖九位镇民从起点可达、移动速度归一化与碰撞、场景/前台/弹窗计龄门禁、每境敌人与首领预载完整、旧续局额外妖物补载、WebP 文件完整及渡劫旧档迁移。TypeScript / Vite 根路径和 GitHub Pages 子路径的独立目录生产构建、Prettier 与 diff 空白检查通过，未重跑战斗平衡模拟（本次未修改刷怪及战斗行为）。
- Chrome DevTools MCP 仅使用隔离存档：实测键盘沿街到坊市和港口、E 打开交谈、买卖玄铁、切换宗门后暂停年岁与研习。茶馆听书从 NPC 接下后自动收起弹窗并继续游历，半年的任务完成后记录本次灵石 +8，未重复接取。入口、交谈与广告弹窗分别取样确认时间不变。模拟 visibility 属性为隐藏并派发 visibilitychange，2.2 秒前后年岁均为 13.100709999999811，无后台补算；这是可见性逻辑验证，不代表真机系统挂起验证。
- 修炼、炼器、入宗、精研、坊市资源不足均弹出正确缺口；取消返回原界面且不消费。真实等待 5 秒领取 300 灵石 / 30 玄铁，旧领取按钮再次点击不重复发奖；返回洞府后主动修炼才扣费，返回坊市后主动买入一枚玄铁才变为 270 灵石 / 31 玄铁。首页不再有常驻物资广告按钮，灵根广告仍保留。
- 对图片 Image.src 设置一次失败桩：城镇显示 80% / 4 项已完成时不能移动与计龄，失败保持遮罩，重试后九位 NPC 完整显示。第三境素材失败时未进入战斗，重试恢复选关与开局；七境逐一切换均完整就绪。此项验证应用错误处理，不声称覆盖真实弱网的全部情形。
- 桌面 1440 × 1000、手机 390 × 844 实页截图检查街巷、港口、入口、宗门及物资弹窗，无横向溢出。手机模拟环境中，合成 touch PointerEvent 走相同拖动处理路径，2.5 秒前进 450 世界像素，停止后可点击附近镇民交谈；暂时桩掉 setPointerCapture 以允许合成事件，不等同于真机手势测试。城镇含行人动画的桌面 179 个有效帧平均 16.67 ms、P95 16.8 ms（无节流，DPR 1），仅为当前设备样本。
- 原有 25 张图片 68,873,273 → 9,217,774 字节，减少 86.6%，逐图校验尺寸及 alpha 无损，PNG 从运行资源移除。新增城镇四张图共 1,360,946 字节。浏览器首境仅请求 8 张 WebP、共 2,511,072 字节，未预载宗门/城镇或其他地图；七境与城镇检查未出现 PNG 请求、丢图、控制台 error / warn。
- 渡劫仅一个境界，突破增量为气血 +100、法宝伤害 +50%。旧初/中/后期快照保留累计修为与已损失气血，重复恢复不重复加成。生产界面合成通关档实测首页显示「渡劫」、HUD 显示「渡劫圆满」；无额外根基的天灵根正道开局气血为 631（基础与境界合计 563，再应用正道 12%）。
- 5173 使用追加资源、原子替换入口发布（`index-4283NGvE.js` / `index-BYZcRECn.css`）；正式入口的隔离页面确认九位 NPC、28 栋建筑、12 位行人及完整加载，旧脚本 `index-apS06WPe.js` 仍返回 200。保留旧 hash 和 PNG 给已经打开的旧页面。本轮尚未提交、推送或发布 GitHub Pages / Cloudflare。

## 人间宗门、供奉与精研（2026-09-20）

- 新增独立人间入口、正魔各八门与两张宗门图集，以及采药、护送、听书和玄铁交易。身份、精研、供奉账期、任务进度和见闻纳入旧档迁移与导入导出，轮回归零。首页显示宗门、流派与当前职务，未入宗或退宗显示散修。职务由九大境界派生，开宗老祖需要解锁并达到渡劫；重新入宗按当前境界授职。
- 在籍时锁定本门路线，不能兼修或选择另一流派；本命不兼容时改用对应灵根的本门入门法宝。新历练自动携带本门一重功法，占四个功法槽之一；精研只增强正向效果，不替代局内三重觉醒条件。离宗保留已完成精研、停用加成；同一时间只有一个宗门。已有不同路线的未结束历练需先结束再入宗，不改变旧战况。
- 供奉按寿元上限的 1/5 安排，借寿计入下一账期，大乘每 5000 年，渡劫立即免供奉。到期余额不足自动清退，未完成研习取消；精研等级保留。突破、借寿不改变已承诺账单，下一周期更新。
- 新增 21 项逻辑测试，最终全量 168 项测试全部通过；类型检查、Vite 生产构建和格式检查通过。覆盖旧档与损坏导入、计龄、准确扣费和余额不足、同刻先扣费后结任务、寿尽与天劫截停、渡劫免供奉、重新入宗、十六功法正向效果、减速/拘魂/骨刺实际技能、气血续局不重复增加、不复活死者、开局赠送和四槽限制、路线锁定与入宗旧局保护。
- Chrome DevTools MCP 隔离合成存档实测：入门扣款、精研完成后保存、切换正魔筛选各八门、正道及魔道首页身份与禁用路线、入门后开局功法一重、先退宗再换宗门。模拟供奉仅差 0.01 年且余额 79/80 时，自动退宗且余额不变，五阶精研保留、未完成研习取消，首页显示散修、三条路线恢复可选。99.99 年角色进入人间后准确停在 100 年，出现原有广告借寿 / 轮回对话框。
- 前台实测推进年岁；开启 AI 的进行中研习在可见性桩隐藏前后均保持年龄 0.9621633333337302、剩余研习 1 年。返回首页后两个采样中年龄与人间时间均保持 10.55541333333346。当前 MCP 隔离窗口切换未产生浏览器 visibility 事件，因此后台边界改用页面可见性属性桩并分发真实应用监听的 visibilitychange 事件：隐藏期间年龄与人间时间均保持 4.7684466666667，无补算。此项是可见性处理逻辑验证，不代表真机后台挂起验证。
- 桌面 1440×1000、手机 390×844 截图检查首页入口、人间概览和正魔宗门卡片，320×568 另查无横向溢出。16 门对应 16 个不同取景区域；两张新增图片均返回 200，页面无控制台 error / warn。素材不参与战斗预加载，人间界面停止绘制战场 Canvas，时间栏每秒更新，供奉/任务变更才重绘卡片。
- 未做真机 iOS/Android 的系统后台测试，未把这些固定边界测试当作长期经济平衡或真人通关率验证。以上为本机实现验收记录；发布前再次执行全量 168 项测试、格式检查和 GitHub Pages 子路径生产构建，均通过。
- 最终版已更新到本机 5173：脚本 `index-apS06WPe.js`、样式 `index-CYqXdGwm.css`。资源追加、入口原子替换，旧 `index-CFClqmcM.js` 仍返回 200；不刷新用户页面、不删除旧 hash 资源。正式入口的隔离存档另查宗门身份、路线锁定和新增图片请求；未操作用户真实存档。

## 手机历练全屏与首页状态顺序（2026-09-20）

- 手机点击开局、续局、迎战天劫时，在点击调用栈内请求根元素全屏，全屏成功后尝试锁定横屏。浏览器缺少 API 或拒绝请求时保持当前显示，不弹窗、不阻塞游戏。电脑不自动全屏。右上角提供「全屏 / 还原」，暂停、升级和结算仍处于同一全屏界面，返回首页解除本次锁定和自动全屏；不退出用户预先开启的全屏。
- 首页「此世灵根」和「年岁 / 寿元」移至秘境选择区域上方，普通首页和通关首页共用该顺序。关卡切换仍同步显示对应时间流速，灵根广告、资质说明和轮回按钮随原卡片保留。
- 新增 10 项显示生命周期测试，总计 147 项通过；TypeScript / Vite 构建、Prettier、diff 空白检查通过。覆盖点击内调用、先全屏后横屏、电脑跳过、拒绝与 API 缺失、请求过程中返回首页、手动退出与再次进入、重复请求合并、用户已有全屏和 WebKit 前缀。
- Chrome DevTools MCP 隔离存档实测：真实点击进入全屏；当前 Chrome 手机模拟环境的横屏锁定返回 `NotSupportedError`，页面保持全屏竖屏，战斗正常推进、无提示弹窗。手动还原及再次全屏可用，切换 AI 导致 HUD 重绘不会再次强制全屏；结算返回首页后退出全屏。续局与天劫入口也实际触发全屏。另以 API 桩模拟横屏成功并检查解锁，以及全屏被拒绝后仍正常开局；这些模拟不代表真机强制旋转验证。
- 截图与 DOM 检查覆盖桌面 1440 × 900、手机竖屏 390 × 844、横屏 844 × 390 和 568 × 320；320 × 568 另查战斗栏无横向溢出。横屏压缩 HUD、法宝栏和升级卡片，暂停按钮等高；升级面板可滚动访问完整配方、重悟和 AI 按钮。568px 横屏的六件长名称仙器及四种功法均正常显示。旋转 / 改变窗口尺寸时 Canvas 更新尺寸并清空旧移动输入。
- 桌面与手机首页截图确认灵根、寿元在历练上方；通关档切换第二境后寿元栏显示「落霞古墟 · 战斗每分钟 25 年」。验证页未出现控制台错误或警告。未使用真机 Android / iOS 验证系统旋转权限，实际能否锁屏由浏览器决定。

## 通关终章首页与天劫止息（2026-09-20）

- 第七境结算后立即清除周期天劫，不再按年岁强制迎劫；已有劫印及其气血 / 伤害加成保留。未通关的大乘仍按原规则迎劫，修为未达到渡劫但已通关的角色同样不再触发天劫。渡劫境闭关可按填写年数出关，不再被两万年截断，也不额外获得修为或属性。
- 本机读档和 JSON 导入共用天劫迁移：旧通关档若正在迎劫，恢复其暂停的原历练；无原历练则回首页，不增加劫印、不重复入账。损坏的原历练仍由导入校验拒绝。
- 通关首页改为青绿暖金终章：角色光环、当前境界、通关题字、修为 / 年岁 / 收藏 / 劫印成果、重游与珍藏入口，以及带各境背景的关卡卡片。最终胜利和结算文案同步体现天劫止息。复用既有图集和地形，新增光环均为静态装饰；未通关首页保持原样。
- 新规则先在旧实现下复现 4 项失败，改后全部 137 项测试通过；TypeScript / Vite 生产构建、Prettier、diff 空白检查通过。测试涵盖即时结算、旧档、灵石修炼、战斗、闭关、劫印保留、正在天劫中的旧档迁移及导入往返。
- Chrome DevTools MCP 使用隔离合成存档验证：旧通关天劫恢复为第三境 83 秒暂停局，原有 30 点缺血保留、倒计时归零；闭关 50,000 年完整出关，劫印仍为 2。未通关大乘旧档仍出现「天劫将至」。
- 受控最终仙尊残血场景实际击杀后出现通关特效与「仙途圆满」结算，七境全通、倒计时归零；返回后自动显示新首页，刷新保留。重游按钮定位并聚焦关卡，珍藏和角色入口可打开相应面板，重新选择第一境可正常开局。
- 桌面 1440 × 900 与手机 390 × 844 截图检查通关首页和结算；320px 与 768px 宽度另查无横向溢出。手机成果分为两列，题字、角色、按钮与数字均完整显示，页面无控制台错误或警告。仅验证功能与布局，本次不调整怪物数值或重新推断通关难度。

## 战斗光效性能与闭关修为（2026-09-20）

- 将弹丸、莲花、雷击、非经验掉落的重复模糊光效绘制到缓存纹理，移动、旋转与透明度继续逐帧更新；缓存限制 96 项。持续扩大的琴音保留矢量线条，伤害数字改用描边。没有调整怪物数量、技能判定、伤害、经验或 AI。
- Chrome DevTools MCP 在 1440 × 900、DPR 2、无 CPU 节流下复现终关合成压力场景（起始 189 怪、110 弹丸、296 掉落、106 特效）。相同固定画面录制 240 帧，改前平均帧间隔 61.92 ms、P95 74.6 ms；关闭模糊的对照约 9.80 ms。缓存改后预热 300 帧为平均 8.33 ms、P95 9.0 ms，该样本缓存像素占用约 1.82 MiB。冷缓存首次生成曾出现约 97 ms 绘制，不能据此宣称消除了所有瞬时卡顿。
- 同一存档恢复到完整生产界面、运行 AI 及存档逻辑各 5 秒，改前平均帧间隔 60.37 ms、P95 75.2 ms，改后 8.35 ms、P95 8.8 ms；改后主线程回调 P95 2.1 ms。固定时刻的前后截图检查技能形状、敌弹、莲花及伤害文字正常。以上为当前电脑的受控压力测试，不代表所有设备的自然战局帧率，也未据此推断胜率。
- 生产页面注入 100 万待消费经验，手动连续选择 15 次，等级 2→17，每次均再次显示 3 个选项，实测响应约 8～38 ms，未出现空弹窗或卡死。自动化测试保持经验封顶、即时入账及续局规则通过。
- 闭关新增随机修为：当前大境界总需求 × 投入年数 / 寿元上限 × 随机 8%～16% × 灵根倍率，整数向下取整。属性抽取概率及 1%～3% 加成保持原规则；大乘起无修为与属性收益，仅快进天劫。预览显示修为范围与百分比，出关显示实际修为、属性和境界，现有存档直接兼容。
- 新行为测试先在旧实现下失败，修复后全套 133 项测试通过；覆盖七档资质、七个有限寿元境界、单次 / 拆分闭关无法从初期突破至中期、短期闭关不能保底刷修为、借寿比例、接近突破时补足修为、突破大乘初始化天劫、保存及导入恢复。类型检查、生产构建、Prettier、diff 空白检查通过。
- MCP 桌面与 390 × 844 手机页面实测：炼气天灵根闭关 50 年预览 14～28 修为（4%～8%），一次实际得到 17 修为、移速 +2%，年岁变为 50，刷新后仍保留。手机输入 40 年即时更新为 11～22 修为及 40% 属性概率；截图检查预览、输入与结果，无横向溢出。控制台无错误或警告，仅使用隔离测试存档。

## 法宝卡片显示仙器配方（2026-09-20）

- 藏器阁的全部 36 件法宝卡片、选中详情、局内领悟 / 升阶 / 觉醒选项共用配方展示：仙器名称、法宝六重及对应功法三重。直接使用战斗已有的 `evolutionPassives`，按当前路线过滤；兼修明确标注两种功法任选其一，不改变觉醒规则。
- 详情明确区分局内重数与永久炼器阶数，升级选项提示条件齐备后还需选择觉醒。手机藏器阁法宝改为两列，桌面升级卡片统一顶部与底部对齐。
- 129 项测试、TypeScript / Vite 生产构建通过。Chrome DevTools MCP 隔离存档验证全部 36 件卡片均有完整配方；正道青霄剑仅显示太玄剑经，魔道追魂钉仅显示天魔解体，兼修青霄剑显示太玄剑经或血煞真经。实际选择万剑归宗后快照中的青霄剑六重、evolved 为 true，恢复战斗。
- 1440 × 900 与 390 × 844 页面验证：藏器阁及升级卡片无横向溢出；手机藏器阁两列各 157px，配方可换行完整显示；桌面三个升级选项的图标、标题及底部位置一致。控制台无错误或警告。未调整战斗、经验、存档格式或法宝数值。

## 仙侠背景音乐与开发指南（2026-09-20）

- 新增原创五声音阶器乐「青岚烟渚」，80 秒立体声 AAC 循环，文件 962,626 字节。预制音轨独立加载，不参与战斗模拟、不加入场景就绪条件；全程只使用一个音乐播放实例。
- 新档默认开声，首次真实点击或按键激活音频。已有静音选择保留，音乐与短音效共用总音量；静音暂停音乐，重新开启继续播放。轮回只重置修行进度，保留声音设置；导入按导入档同步声音设置。
- 129 项测试、TypeScript / Vite 生产构建、Prettier 和 diff 空白检查通过。新增测试覆盖新档默认开声和旧档显式静音兼容。
- Chrome DevTools MCP 隔离页面实测：操作前不创建音频；点击与键盘都可激活，AudioContext 为 running，媒体时间前进、音频分析器输出非零。音量 0 时输出采样为 0，调至 30% 后总增益为 0.3；将进度移至曲尾后自动回到开头并继续播放。
- 静音后媒体暂停且输出为 0；刷新后仍静音、30% 音量保留，普通按键不会重新开声。切换洞府、返回大厅、开局和暂停期间仅一个音乐实例，播放时间连续。隔离档实际确认轮回后声音仍关闭、音量仍为 30%，年岁及修为清零。无未处理 Promise 拒绝或控制台错误。
- 桌面及 390 × 844 手机截图确认音量面板文字、滑杆和静音按钮正常。手机页面宽度 390，面板左右边界为 190.5 / 330.5，无横向溢出。这里验证的是浏览器音频链路与布局，未用真人试听评价音色，也未覆盖所有手机系统。
- 新增根目录 `AGENTS.md`，说明开发目录、检查命令、存档兼容、MCP 浏览器验证、本机 5173 保留旧资源的发布方式，以及 GitHub Pages / Cloudflare 的独立发布流程。

## Cloudflare 部署（2026-09-19）

- 新建 `qinglan-xiuxian` Cloudflare Pages 项目，生产分支为 master，正式地址 `https://qinglan-xiuxian.pages.dev/`。通过 Wrangler 上传 28 个构建文件，服务纯静态站点，不增加后端或改变浏览器存档。
- 新增 Wrangler 配置和 `build:cloudflare` / `deploy:cloudflare` 命令，使用独立目录 `artifacts/cloudflare` 与根路径 `/`。不覆盖本机 5173 使用的 dist，GitHub Pages 继续按仓库子路径自动构建。
- Cloudflare 构建的脚本为 `index-COG8J6_u.js`，与本机生产构建一致；GitHub Pages 对应脚本为 `index-DKA6Zrzm.js`，工作流 35449967472 成功。格式检查与 Cloudflare TypeScript / Vite 构建通过；安装 Wrangler 未变更已有依赖版本，令牌只从本机环境读取。
- Chrome DevTools MCP 确认 Cloudflare 生产地址首页、脚本、样式、地图及新天劫立绘请求返回 200。首次验证遭遇网络切换错误，重新加载后恢复请求；当前网络下首次下载全部素材较慢。

## 闭关修炼与两万年天劫（2026-09-19）

- 洞府新增自填闭关年数，支持一位小数。整次获得属性提升的概率为消耗年岁 / 寿元上限，没有额外概率上限；100 年寿元投入 50 年即 50%。成功随机给气血、法宝伤害或移速中的一项增加 1%～3%，不扣灵石、不增加修为或根基等级。寿元不足拒绝且不扣资源。大乘及渡劫境只快进年岁，在天劫时截停；存档、导入与轮回覆盖新增加成。
- 修习根基同时消耗灵石与年岁，天 / 异 / 二系普通 / 三系普通 / 四系伪 / 五系伪 / 无灵根每阶分别耗 1 / 1.2 / 1.4 / 1.8 / 2.5 / 3.3 / 5 年。寿元不足或天劫待处理时不能修习。续局保留已损失气血并应用新属性。
- 从突破大乘时起每两万年触发独立天劫，旧大乘档从当前年龄起算。保留原历练，仅生成一个新首领，不掉经验、不刷怪、不计龄；落雷、缺月三环、交叉雷柱与收束外圈配合瞄准雷弹、环射、交叉弹幕。先避招再在核心窗口输出，第五劫每窗口最多扣 3% 首领气血。核心开放只清除敌方攻击，玩家已布置的法宝区域保留。
- 胜利每次永久气血 +3%、伤害 +2%，只发一次；返回原历练。战败可用 10 次广告复活，刷新不重置次数、首领与弹幕；主动放弃必须再次确认，随后轮回清空本世。独立天劫不增加七境完成记录，也不发普通妖王经验、遗宝与结算奖励。
- 128 项测试、TypeScript / 生产构建、格式检查通过。覆盖七档根基耗时、寿元临界与资源不足、旧档迁移、战斗触发、闭关自填校验、50% 抽签边界和 80% 概率、三个属性实际生效、独立首领和经验隔离、雷弹实际移动与命中、护盾与窗口伤害上限、奖励幂等、暂停恢复和导出导入。
- `npm run balance:tribulation`：天灵根、满阶根基、十阶炼器、六件常用觉醒法宝与四种满重功法，五次天劫各 9 个固定种子。前四次共 36 个样本全部获胜（部分需 1–2 次复活）；第五次 9 个样本均在用尽 10 次复活后失败，首领剩余 34%–49% 气血。仅是这一构筑的 AI 样本，不保证真人无法通过。
- Chrome DevTools MCP 隔离浏览器实测 1440 × 900 与 390 × 844：自填 7.5 年正确扣龄、不扣灵石；100 年寿元自填 50 年显示 50%，实测抽到移速 +2%，刷新后年岁 70 与加成保留。手机输入与按钮无横向溢出。大乘输入 20,000 年、距天劫 50 年时，实际只扣 50 年并立即进入迎劫弹窗。
- 浏览器实测进入天劫后只有一个首领，地面标记和飞行雷弹均正常绘制，原关卡与 80 秒进度独立保存，渡劫不计龄。胜利实测发一枚劫印，原关卡恢复 80 秒并保留 30 点气血损失。广告结束后复活次数由 2 增为 3，刷新仍保留首领 1,000 气血与复活次数；放弃确认后年龄、修为、物资、劫印、闭关加成和原历练一起清零。洞府直接进入、无原历练的天劫也能刷新恢复。
- 本机 5173 保留原 preview 进程及旧 hash 资源，仅原子更新入口。验证全部使用隔离存档，未刷新用户页面或修改真实进度。

## AI 躲避所有妖王冲撞（2026-09-19）

- 原 AI 只避让妖王当前位置，无法识别锁定后的长距离冲撞路径。新增按剩余冲程计算的危险带，仅在危险带附近比较 16 个移动方向，预测随后三次 120ms 决策的位置，优先横向离线，并留出余量防止拾取目标把角色拉回路径；同时评估所有正在冲撞的妖王，普通移动与选技逻辑保留。
- 117 项测试、类型检查、生产构建通过。新增复现用例在修复前失败；覆盖七王五个方向、狼王近距离与连续三次冲撞、两王相向与交叉、冲刺中开启 AI、续局、死亡首领与已离开剩余冲程等情况，使用实际战斗 update 与 120ms 决策延迟核对受伤事件。
- Chrome DevTools MCP 隔离存档实测第三境狼王近身预警：约 0.62 秒内角色横向移出约 86 像素，随后完整冲撞结束气血仍 100 / 100，未使用无敌。实际加载产物为 `index-CaYiSPfT.js`，不操作用户真实对局或存档。
- 固定种子前六境模拟：首境 21 个样本 18 胜 3 败，三条路线连续六境 54 / 54 通关；终关四种起始境界共 12 个样本 1 胜 11 败，AI 仍可能被其他招式或围攻击败，不保证通关。妖王数值、技能、预警时长与第七境难度未改。

## 前六境伤害与妖潮强化（2026-09-19）

- 前六境普通怪刷新量 +25%，自然刷新上限保持 240，四批兵种时机不变；普通怪伤害 ×1.3，精英伤害 ×2.2，妖王基础伤害改为 52 / 68 / 84 / 100 / 116 / 132。仅调整前六境，终关沿用原密度、精英与七王伤害；经验、境界奖励与怪物血量未调整。
- 112 项测试、类型检查与生产构建通过。新增测试覆盖六境三个难度下实际碰撞扣血、妖王弹幕命中和预警区域伤害、刷新增幅及自然刷新上限，并核对终关所有兵种与七王的原伤害及续局保存。伤害和刷新量用例在修改前均能复现未强化。
- 固定种子 AI 模拟：首境 21 个难度 / 本命样本 18 胜 3 败，其中两次为最高难度、一次为魔道玄砂；普通难度三条路线各三种子连续六境 54 / 54 首次通关。均使用天灵根，仅作为回归样本，不代表真人或所有灵根胜率。
- Chrome DevTools MCP 在隔离存档实际推进至第一境妖王登场，普通难度读取伤害 37.44（52 × 0.72），控制台无错误或警告。最终本机 5173 产物为 `index-Dxl8Sg5f.js` 与 `index-Dvj5ehd8.css`，保留此前灵根气血、回血和仅寿尽借寿改动。

## 灵根气血与回复差异（2026-09-19）

- 天 / 异 / 普通 / 伪 / 无灵根基础气血分别为 100 / 95 / 90 / 85 / 80，基础回血为每秒 0.18 / 0.16 / 0.14 / 0.12 / 0.10 点。长生真经每重另加每秒 0.2 点，正道对合计持续回复乘 1.2；境界、淬体、功法与正道气血加成继续叠加。大厅、广告选根和图鉴同步显示基础数值。
- 108 项测试、类型检查、Vite 构建与格式检查通过。覆盖七档灵根、三条路线、功法叠加和真实 update 回复量；旧续局保留开局资质与已损失血量，重复读档不补血，死亡存档不复活。
- Chrome DevTools MCP 隔离页面在 390 × 844 和 1440 × 900 实测并截图：无灵根显示 80 气血与 0.10/秒，天灵根显示 100 与 0.18/秒，自选广告说明可完整阅读，无横向溢出。全局已洗为天灵根的旧三系续局仍从 100 上限 / 67 气血迁移为 90 / 57，进入战斗正常缓慢回复。
- 手机端为寿元新增行调整计时器、妖王血条与提示位置，玩家栏底部 134.5px、计时器顶部 138px，不再重叠。首页与洞府仍不显示提前借寿按钮。
- 本机 5173 产物为 `index-CQ6bgn_9.js` 与 `index-Dvj5ehd8.css`；发布保留旧 hash 资源与原 preview 进程，未操作用户实际存档或对局。

## 借寿入口仅在寿尽显示（2026-09-19）

- 移除首页与洞府寿元卡片的常驻借寿按钮，同时限制广告只能从寿尽弹窗进入。寿尽续命、每次基础寿命 +30% 与轮回规则保留，升级和 Boss 奖励未调整。
- Chrome DevTools MCP 实测首页和洞府借寿按钮均为 0；100 岁炼气存档显示唯一借寿入口，点击正常进入广告，取消返回寿尽选择。1440 × 900 和 390 × 844 截图检查寿元卡片无残余空位或水平溢出，轮回按钮仍在此世灵根卡片内。
- 类型检查与 Vite 构建通过。本机 5173 发布产物为 `index-CeXP_L9M.js` 与 `index-Dr6New8G.css`。

## 五行灵根、寿元、存档迁移与关卡提速（2026-09-19）

- 本机 5173 正式入口已更新为 `index-DKZZWT_n.js` 与 `index-BkTKaLbl.css`，原 preview 进程继续运行，旧 hash 资源保留。只在隔离上下文操作合成测试存档，没有读取、清空或刷新用户实际对局。
- 105 项测试通过，类型与生产构建通过。五行测试覆盖全部 36 件法宝的未觉醒 / 觉醒实际伤害、七档资质、匹配与不匹配属性、持续和派生伤害、十阶炼器与悟道的组合；续局弹道不重复放大。天灵根 +15%，异灵根 +12%，二 / 三系普通灵根 +8% / +6%，四 / 五系伪灵根 +4% / +3%，无灵根不加伤。
- 金木水火土分别有正魔入门法宝；新建与广告自选按首位五行设置本命，旧收藏、炼器保留。七种资质广告可选对应数量的互不重复五行，天灵根也能重选；真实页面验证选项不完整时，即使移除 disabled 也不能领取，完整等待后生效。旧续局保留开局资质、五行与法宝。
- 悟道每阶加成为天 / 异 / 普通 / 伪 / 无灵根的 5% / 4% / 3% / 2% / 1%，已有等级不重置；测试验证当前局锁定资质。导入三系普通灵根后，洞府真实显示悟道 10 / 20 阶、每阶伤害 +3%。
- 前六境倒计时改为 3 / 4 / 5 / 6 / 7 / 8 分钟，四批敌人仍按 0% / 25% / 50% / 75% 解锁，精英间隔压缩至 36–48 秒。边界测试验证新时间点出王且不重复；旧续局按进度比例压缩时间与精英计时，只转换一次。第七境仍为十分钟、固定时点七位 Boss。
- 年岁按七境每分钟 10 / 25 / 50 / 100 / 200 / 500 / 1000 年增长，跨局累计；旧档从零计龄，暂停、升级、死亡与广告不增长。测试覆盖全部大境界寿限、突破延寿、大乘无限、十分钟炼气寿尽、借寿累计、存档往返不重复扣龄。
- 实际浏览器从 99.99 岁恢复并继续战斗，100 岁时冻结，显示借寿或轮回。五秒前强行点击无奖励；完整广告后年龄仍 100、额外寿元 30 年、十阶炼器保留、满血与三秒护体、普通复活次数仍 0，并先暂停。刷新后借寿和暂停对局保留。寿尽存档刷新仍显示选择、Esc 不能绕过，取消广告不改变寿元；放弃续命后资源、修为、炼器、年岁、借寿与续局全部重置，刷新仍是新生存档。
- 导出按钮实际产生带格式、版本和时间的 JSON，包含当前全部存档与续局。导入校验覆盖格式、版本、必填数据、负数、非法灵根、坏续局和大小上限。浏览器实际验证坏文件、取消确认、模拟存储空间不足都不覆盖旧档；正常导入恢复修为、三系灵根、86.75 年岁、60 年借寿、十阶炼器、待拾取、1:20 续局及伤害统计。MCP 文件上传受工具工作区白名单阻挡，因此通过同一 MCP 在页面构造 File / DataTransfer 触发真实导入事件，未验证系统原生文件选择器；没有使用其他浏览器工具。
- 1440 × 900 与 390 × 844 截图验证：轮回按钮在「此世灵根」卡片内，页面底部无独立按钮；暂停四按钮桌面同排、手机两列，全部高 52 像素；遗宝拾取按钮中心误差约 0.004 像素。灵根、自选广告、寿元、续命、导入确认与藏器阁五行标签无水平溢出。正式 5173 出发按钮可用，未发现控制台错误或警告。
- 固定种子 AI 模拟：新档第一境三档难度共 9 / 9 通关，12 个其他本命样本 11 / 12 通关（魔道玄砂样本在 138 秒失败）；三路线各三个种子连续前六境 54 / 54 首次通关。这些样本使用天灵根，未覆盖所有低资质、五行及真人操作，不代表通用胜率；边界注入只作功能验证。

## 灵根、广告补给与妖王追袭（2026-09-19）

- 首次创建角色与轮回按六档概率随机灵根，影响局内经验与永久修为；天灵根沿用原速度，旧档保留天灵根。主页、洞府与指南展示倍率；五秒占位广告可获得天灵根或领取 300 灵石、30 玄铁。
- 七位妖王均增加距离超过 330 像素时的独立追袭，预警 0.85 秒，沿锁定方向冲刺 574 像素；普通追袭间隔 9 秒，半血后 6.5 秒。狼王与仙尊原有突进同步延长，仙尊雷袭保留一秒预警。预警带显示实际冲刺宽度与终点，读取续局时重建预警。
- 全部 91 项测试通过，类型、全库格式、diff 检查及默认根路径和 Pages 子目录生产构建通过。新增妖王测试先在旧逻辑下失败，再验证七王预警静止、方向锁定、位移与终点一致、距离及冷却限制、半血加快、原招式不打断冲刺、续局兼容；普通冲刺速度不变。灵根测试覆盖六档概率、全部三档难度、普通经验、Boss 经验与修为、结算、旧档迁移及洗灵根后续局不重复入账。
- Chrome DevTools MCP 独立上下文实际验证：固定随机值创建五灵根，刷新改随机值后仍保留五灵根；完整看广告后成为天灵根并刷新保留。未满五秒即使移除按钮 disabled 也无法领，Esc 取消无奖励；两轮物资共得 600 灵石、60 玄铁，同次重复点击只领一次，取消或未领取就刷新不发奖。领取后正确返回洞府。
- 天灵根角色轮回后实际重抽四灵根，资源、收藏、续局按原规则重置。受控五灵根未完成对局在主页洗成天灵根后，续局仍为五灵根，永久修为始终为 2，没有追溯补发。原复活广告实际完整等待五秒后恢复满血，次数变为 1，续局资质不变。
- 1440 × 900 桌面和 390 × 844 手机截图检查灵根卡片、奖励按钮、广告页、资质指南，无水平溢出。受控妖王续局实际显示宽预警带、蓄力阶段原地不动，继续运行后完成长距离突进；控制台无错误或警告。这些边界场景用于功能验证，未据此推断所有资质与搭配的自然通关率。

## 十阶炼器（2026-09-19）

- 永久炼器上限改为十阶，前五阶费用保持不变，第六至十阶的玄铁与灵石费用加速递增。每阶伤害仍加 8%，满阶为 +80%；存档加载保留六至十阶。
- 新增回归测试先在五阶旧逻辑下失败，再验证 36 件法宝逐阶扣款、十阶上限、每阶刷新保存、缺少任意一种材料不扣款、旧五阶与十阶实际攻击加成。全部 83 项测试、TypeScript、根路径与 Pages 子目录构建通过。
- Chrome DevTools MCP 实际将五阶青霄剑连续升至十阶：消耗 780 玄铁、11650 灵石，刷新保留 10 / 10 和 +80%，满阶按钮禁用。390 × 844 手机截图验证等级标记、详情、费用和按钮，没有文字越界或水平溢出。

## GitHub Pages 路径兼容验证（2026-09-19）

- GitHub Actions 工作流对 master 推送执行格式检查、测试、类型检查、构建和 Pages 部署。Actions 版本按官方发布记录核对并固定到 commit；Node.js 使用项目支持的 22 版本，发布路径读取 configure-pages 的 base_path。
- `npm test` 80 / 80 通过，TypeScript 检查通过；分别构建默认根路径和 `/qinglan-xiuxian/` 子目录版本。
- Chrome DevTools MCP 在隔离上下文访问 5173 上的临时子目录产物：七张地图、全部人物图集、法宝图集、脚本、样式、图标与后台 Worker 均请求子目录地址并返回 200；实际进入战斗、暂停并查看指南正常，无素材加载失败或控制台错误。截图检查战斗与暂停界面。
- 默认根路径构建同样通过浏览器验证，妖物志 79 个角色图集引用仍使用 `/assets/`，未出现加载失败。
- 用户确认公开仓库后，Pages 已开启并强制 HTTPS，[首次发布流水线](https://github.com/chenxuan520/qinglan-xiuxian/actions/runs/35440195600) 成功。实际访问 [线上游戏](https://chenxuan520.github.io/qinglan-xiuxian/)，全部地图、人物、法宝与 Worker 请求返回 200，进入战斗推进时间、暂停与保存正常，控制台无错误或警告。首次素材加载约 33 秒，此为本次网络环境实测。

## 当前版本：通关特效、性能、复活与战绩（2026-09-19）

本机发布至 `http://localhost:5173/`，后续版本同时通过 GitHub Pages 提供访问。通关特效版本正式入口加载 `index-DfpgEupG.js` 与 `index-49xAofQD.css`；原 preview 进程未重启，保留旧 hash 资源，发布仅原子替换入口，不刷新正在游玩的页面。

### 通关特效

- 普通关卡显示「一剑荡妖尘」，第七关显示「七劫尽破」和七劫印记。八柄金色剑影随法阵展开，24 道灵光散开，约三秒后自动进入原结算页；点击查看战绩或按 Esc / P 可立即跳过。
- 特效仅在成功结算时触发一次，奖励先保存。使用合法续局快照，将最后 Boss 调为一血后由实际法宝斩杀；普通关与终关在动画期间的 localStorage 已记录通关、runs 为 1、activeRun 为 null。这是受控界面验证，不作为自然通关或平衡性证据。
- 正式 5173 实际加载新脚本并触发「七劫尽破」；在特效中途刷新后，七境通关记录保留、runs 仍为 1、activeRun 为 null，没有重复结算或残留续局入口，控制台无错误或警告。
- Chrome DevTools MCP 检查 1440 × 900 桌面和 390 × 844 手机截图；标题、剑阵、七劫文字、跳过按钮可见，手机无水平溢出。自动进入结算、按钮跳过、Esc 跳过均正常。跳过后立即开始新局并暂停，等待旧动画时限后没有残留结算弹窗，runs 仍为 1。
- 动画仅使用有固定时长的 CSS transform / opacity，战斗 Canvas 继续沿用结束状态冻结策略；系统减少动态效果规则可关闭动画。没有新增图片下载、依赖或逐帧特效循环。

### 验证结果

- `npm test`：80 / 80 通过；TypeScript、Vite 独立目录生产构建、Prettier 与 `git diff --check` 通过。
- 36 件法宝分别验证未觉醒与觉醒攻击、持续区域、分裂、弹射、爆炸的伤害归属，以及 JSON 续局。伤害取实际扣血，护盾减伤及过量击杀不会虚增；骨甲反伤单列，旧存档历史伤害不伪造来源。
- 复活每局最多十次、满血与三秒无敌、同次死亡重复调用无效、续局保留次数、拒绝非法次数；连续复活至第十次后再次死亡不能复活。与最后妖王同时倒下也记录击败进度，复活后可正常通关；已入账修为不会因复活或结算重复发放。
- 七位妖王按固定时间自动出现，前王存活不阻塞下一位，七王乱序击杀必须全部完成；旧串行存档恢复时补齐已经到点的妖王，重复读档不重复出王。
- Boss 经验至少为本关最强精英的三十倍，独立永久突破奖励按关卡发放，不受后期小怪折算削减；100 级封顶与第七关渡劫门槛继续通过。

### 连续升级卡死与正道气血修正

在正式旧版 5173 中注入正常三选一、LV 61、十万储备经验、手动模式的续局后，实际选择第一张卡。350 ms 后读到 `state=upgrade`、LV 62、仍有两条候选，但 DOM 中选择卡数量为 0、弹窗为空，稳定复现用户所述卡死。原因是 `choose` 将逻辑状态设回 playing 后未同步 `previousState`，下一帧直接再次进入 upgrade，界面误认为状态未变化；AI 仍可按约 0.9 秒节奏调用选项，所以停顿后自行恢复。

修正选择完成时的状态同步。Chrome DevTools MCP 复测相同快照：手动选择后 LV 62 / 63 均可见下一轮两张卡，选满后仅剩一个机缘自动领取，正常回到 playing；AI 模式连续采样 LV 61–64，所有 upgrade 状态都有可见选择卡，无隐藏暂停。修复已随 `index-CQc7Tv4c.js` 发布至正式 5173。

正道气血由固定 +12 改为总上限 ×1.12，计入基础、根基、境界与气血功法，四舍五入为整数。新增测试覆盖后期根基、功法领悟、当局突破和旧快照迁移；正道旧 542 / 575 气血续局在正式 5173 显示 598 / 631，保留已损失的 33 点，重复恢复不额外补血。路线说明实际显示「气血上限 +12%」。

正道持续回血由固定每秒 +0.2 改为「基础回血 + 功法回血」整体 ×1.2。测试覆盖功法 0 / 3 / 5 重，确认正道回复量比相同基础的魔道与兼修高 20%，并验证实际推进一帧后的气血变化。路线、指南与 README 同步说明；页面实际显示「持续回血速度 +20%」。

### Chrome DevTools MCP 页面验证

全部操作在独立测试上下文，未访问用户游戏存档。

- 正式 5173 页面实际恢复死亡存档，出现「看广告复活」。点击后显示「广告位招租」，五秒前按钮禁用，结束后满血复活。正式版本存档记录气血 563 / 563、复活次数 1、结算次数 0，随后暂停并刷新仍保留次数。再次结束对局结算一次，清除 activeRun。
- 十次复活版本的候选页面显示「剩余 10 次」；恢复已用九次的死亡存档显示「剩余 1 次」，实际等待广告后复活为 563 / 563，保存次数为 10，未结算。恢复已用十次的死亡存档直接进入结算，无复活按钮，结算次数为 1，清除 activeRun。
- 桌面与 390 × 844 触屏模拟截图检查伤害排行；六件法宝名称、总伤害、占比和条形图可见，无水平溢出。结算初始焦点不再把页面滚到底部。旧历史伤害以未分类显示。
- 战斗顶部「伤害」和暂停菜单「伤害统计」实际打开同一排行；打开时冻结战斗，从战斗打开后点击关闭或按 Esc 均恢复 playing，从暂停菜单打开后关闭仍为 paused。桌面与 390 × 844 手机截图确认入口和六条排行可见，手机无横向溢出。
- 七位实际调度生成的 Boss 同时渲染，HUD 显示「同场 7 位妖王」，各自立绘与血条正常。该场景只调整了摆放位置用于截图，不作为自然通关证据。
- 升级弹窗开启后背景不再持续重绘；选择法宝后绘制恢复，实测选择处理 1.5 ms。暂停、升级和结算冻结画面，窗口 resize 仍重新绘制；AI 选择计时与后台模拟仍由原逻辑推进。

### 性能证据与限制

使用同一个第七关 607.9 秒快照（140 敌人、116 弹道、16 区域、121 掉落、六件法宝），对比发布前代码与优化代码。优化缓存按显示尺寸裁剪的角色图片和精英法阵，裁掉视野外区域，AI 最近掉落改为一次遍历，碰撞与寻敌比较使用距离平方。

- Node 固定种子片段每次推进 120 帧，预热后 35 次平均：24.86 ms → 16.64 ms，约减少 33%。两版结束时敌人数、弹道数、斩妖数、气血与角色坐标一致。
- Chrome 1440 × 900、DPR 2 的固定战场，缓存优化前后 rAF 中的绘制指令平均耗时约 1.18 ms → 0.81 ms，约减少 31%；帧间隔均约 8.3 ms，不能据此宣称实际帧率提高 31%。
- 升级界面等待两秒，旧版产生 32620 次 drawImage，冻结背景后为 0；进入选项仍绘制一次，返回战斗立即恢复。等待阶段未观察到超过 50 ms 的主线程长任务。
- 初次多测试页同时运行的采样受其他页面影响，已排除；强制像素读回测量会改变 Canvas 流水线，未作为用户帧率结论。没有取得真实低端设备或手机 GPU 的性能证据。

固定种子终关模拟（不使用广告复活）共 12 组，1 组在 628 秒自然通关，其余在 375–630 秒失败；所有样本未超过 100 级。Boss 奖励使成功斩王的低境界角色能更快突破，未通过终关仍卡在大乘后期。此为有限 AI 搭配样本，不代表真人胜率。

## 历史版本：七劫终关、分批妖潮与成长上限（2026-09-19）

最终版本仅发布到本机 `http://localhost:5173`，生产脚本为 `index-Dp8mL0iD.js`，样式为 `index-B0dqvPYn.css`。5173 原 preview 进程未重启；发布先复制资源，再原子替换首页，旧 hash 资源保留，已打开的游戏不会被强制刷新。5174 和 5273 已停止服务。本轮没有提交或推送 Git。

### 自动验证

- `npm test`：70 / 70 通过。`npx tsc --noEmit`、Vite 独立目录生产构建、`npm run format:check`、`git diff --check` 通过。
- 新增回归测试先在旧逻辑失败，再验证通过：四批怪物的时间边界、全部十二种可刷出、后续新批次约占 65%、定时精英不提前引入未开放兵种、后半场双精英；终关在 0:00 / 0:45 / 1:30 / 2:30 开放四批。
- 验证终关密度、移速、伤害和施法压力递增；自然刷新和召唤遵守 210 同屏上限。十分钟、二十分钟、一小时生成的同类小怪属性和自然刷怪速度一致，不再因拖延无限增强。
- 验证前 60 级经验曲线不变，后期门槛按原曲线乘以 1 + (等级 - 60)² / 180 递增，100 级只领取最后一次机缘，之后大量溢出经验不再升级回血；最后 Boss 直接结算的经验也不突破上限。超过 100 级的旧续局限制到上限，不清空装备、永久修为与已结算收益。
- 验证七位 Boss 的时序、不叠王、续局、旧十二分钟存档迁移、最终仙尊独立立绘、六种招式循环、220 基础伤害、精英天兵和一秒预警突进、通过终关才解除渡劫瓶颈、强敌永久修为在高境界折算减缓。
- 验证终关不掉落回血丹，旧续局丹药移除，宝匣只给玄铁和法宝升阶；前六境回血丹和宝匣效果不变。
- 验证默认两件本命、Boss 掉落未拥有的三件法宝、拾取后炼器、旧档迁移、待拾取遗宝存档、音量合法范围和单机缘自动领取。现有战斗、觉醒与存档测试继续通过。

### 实际页面验证

浏览器操作全部使用 Chrome DevTools MCP 的独立上下文，未操作用户实际存档。

- 最新 5173 页面实际加载 `index-Dp8mL0iD.js`；新旧脚本、独立仙尊立绘及终关地面请求返回 200，无 console error / warn。受控旧 200 级存档续局后显示 `LV. 100`、`已满级`，2:30 时 HUD 显示「妖潮 4 / 4」，已截图检查。
- 九个境界分别注入边界修为，捕获并逐张检查实际 Canvas：灵气、三剑阵、悬丹、青莲元婴、五行符、虚空星环、阴阳灵环、万剑法阵、渡劫雷光均不同。1440 × 900 桌面与 390 × 844 触屏模拟已检查；移动页面无横向溢出，第七关卡文字不裁切。
- 仙尊战场已截图检查透明立绘、地面和血条。受控一血仙尊由真实攻击击败，结算写入第七关通关、解除渡劫瓶颈并产生三件遗宝；实际点击拾取后收藏由两件变成五件、待拾取清空。
- 洞府底部「轮回转世」实际可见，点击后有二次确认；取消不改变存档，确认后境界、资源、关卡、收藏、炼器和续局清空，只保留青霄剑与追魂钉，刷新仍保持新档。
- 音量实际调至 27%，关闭音效再开启和刷新均保留 27%。
- 终关受控残血存档恢复后，旧回血丹移除；拾取宝匣后青霄剑由一重升二重、玄铁 +2，气血仅由 200 变为 200.03123（正常被动回复），没有宝匣回血。旧 200 级恢复为 100 级且溢出经验为零。
- 最新妖物志实际显示仙尊全部六招，已截图检查新增文字无裁切；新旧脚本及样式请求均返回 200。
- 后台验证使用受控 `document.hidden` 和停止后续 rAF 回调：Worker 在约 30.8 秒内推进约 29.1 秒战斗，角色移动、自动选技与保存继续；主动暂停保持时间不变，手动模式失焦暂停。这不是系统冻结或真实手机后台验证，不提供离线模拟。

### 固定种子模拟

最终 `npm run balance:trial` 使用根基 10 / 10 / 8、六件常用法宝五阶、兼修、初入仙途，四档境界各三个固定种子。5 分钟为 89–92 级；到达 10 分钟的样本均为 100 级，没有突破上限。移除终关掉落回血、限制 100 级并强化仙尊后，12 个固定 AI 样本全部失败，结束在 369–709 秒，其中 7 个到达最终仙尊；大乘后期三个样本分别在 709、505、379 秒失败。这版终关明显更难，尚未取得固定 AI 自然通关证据；用户明确要求直接发布自行试玩，未据此擅自削弱敌人。

前六境三路线连续模拟共 54 局均通关；12 个单独新本命样本中驻地灵塔一组在第一境 220 秒失败，其余通关。不同起始境界会改变击杀顺序与随机搭配，样本胜率不随境界严格单调；以上是有限种子的 AI 结果，不代表真人胜率或所有搭配。浏览器中的边界存档只用于功能与画面验证，不作为难度证据。

## 历史版本：大境界收益与地域妖物（2026-09-19）

`npm test` 43 / 43 通过；`npm run build`、`npm run format:check`、`git diff --check` 通过。

已发布到本机 5173 与 5273：两端加载同一生产脚本 `index-we56_pv-.js`，开发 hook 均不存在。Chrome DevTools MCP 独立上下文验证 5273 开局、暂停和六境图鉴各 12 种；5173 的旧元婴存档由 90 / 127 气血恢复为 216 / 253，连续刷新仍为 216 / 253，修为仍为 2644。五张新增素材均返回 200，两端无 console error / warn。原有 preview 进程未重启，已打开页面仍引用原脚本且旧脚本请求保持 200。未操作用户实际浏览器存档。

本轮按最新需求替代累计妖物池：六境各 12 种专属妖物，合计 72 种普通妖物及 6 位妖王。新地图只刷新本境普通怪、精英和召唤物；旧版续局已在场妖物仍保留，后续刷新使用新规则。

- 大境界突破由气血 +3、伤害 +2.5% 改为气血 +45、伤害 +35%；小阶段收益不变。元婴初期累计境界加成为气血 +153、伤害 +120%。
- 三项新增测试先在旧逻辑上失败，再验证八次大境界边界、当局突破和新局一致、结算突破、旧元婴存档补齐气血及重复读档不重复发放。已有死亡不复活和修为差额结算测试保持通过。
- 妖物测试验证每境恰有 12 种、六境互不重叠、全部普通怪可刷出、精英与召唤物不跨境，72 个普通怪与 6 位妖王的立绘映射唯一且不越界。
- 五张新增图集通过 PNG 尺寸和真实 alpha 通道检查；Chrome DevTools MCP 验证五张均返回 `200 image/png`。图鉴实测六境各有 12 张普通怪卡片。桌面图鉴、天境战场、390 × 844 移动图鉴已截图检查，无横向溢出；古墟窄幅图集修正了图鉴拉伸和战场高度。
- 洞府说明实测显示元婴初期气血 +153 / 法宝伤害 +120%，与战场气血上限 253 一致。
- `npm run balance` 三档第一境各三个固定种子全部通过；12 个新本命路线样本全部通过。三路线各三个种子的六境连续模拟共 54 局全部首次通关，天境耗时 610–636 秒，其中魔道一组最低气血 36。每局采用实际结算、局间修炼与炼器，没有额外加血或攻击作弊。

模拟仅代表固定种子的 AI 策略，不等于真人胜率；六境连续样本为「初入仙途」，没有覆盖所有流派搭配与高难度六境。浏览器战场注入仅用于立绘与边界验证。开发与验证在独立 5174 端口进行，发布时先复制完整资源再替换首页，保留旧资源以免打断已打开的游戏。

## 历史版本：082f538（以下记录保留，不代表当前怪物池）

日期：2026-09-19。环境：Node.js 25.8.2、Chrome DevTools MCP。最终验证在独立工作树与 5174 测试端口进行，游玩中的 5173 服务未切换。

## 逻辑与构建

- `npm test`：40 项测试通过。
- `npm run build`：TypeScript 检查与 Vite 生产构建通过。
- 36 件法宝分别验证基础伤害、觉醒后的攻击及 JSON 快照恢复。
- 验证正魔各 18 件法宝、8 种功法，纯修选技池和本命限制、兼修混搭、流派配方、携带上限，以及 AI 识别配套与觉醒。
- 验证残血加速、承伤代价、骨刺反击无敌间隔、命中减速、拘魂距离、斩妖回复、玄伞消弹、灵塔锁敌、棺落延迟伤害、裂镜只分裂一次、残血斩镰。修复致死伤害后被被动回复或地面丹药意外复活的问题。
- 验证旧存档兼修兼容、已有跨流派装备保留、大厅改路线不改变已开始对局、非法本命修正；原实时修为和差额结算测试继续通过。
- 28 种普通妖物与 6 位妖王使用 34 个独立图集区域。测试逐境妖物池包含之前所有种类，并覆盖召唤、散射、环射、护盾减伤。
- 52 件法宝 / 功法全部映射到不同的生成图片区域；测试无 SVG 物品图标。另检查所有场景与人物图集路径真实存在、PNG 头与尺寸有效，防止新增引用缺图。

## 浏览器验证

只使用 Chrome DevTools MCP，采用独立浏览器上下文：

- 桌面 1440 像素宽：法宝三页共 36 个唯一图片区域，16 个功法图片、正魔筛选、路线按钮和新妖物志均已截图检查。
- 移动模拟 390 × 844：路线选择、法宝分页、正魔功法、妖物秘境筛选、觉醒卡片、暂停和续局均已检查，无横向溢出。
- 切换法宝 / 功法 tab 及流派筛选，弹窗外层、section 与原有动画对象均保持相同，面板位置和尺寸不变。修复了整层弹窗重建导致淡入重播的问题。
- 魔道开局保存路径、伤害倍率 1.12 与 HUD 显示一致；切换流派会为不兼容的本命选择同流派法宝。
- 受控觉醒场景：青霄剑六重与血煞真经三重进入优先觉醒选项，实际点击后觉醒；刷新后恢复觉醒和原路线。该场景为兼容已有装备的旧局，当前魔道新局不会获得青霄剑。
- 大厅改成正道后继续先前魔道对局，恢复的对局仍保持魔道并处于暂停。
- 受控太虚场景同时显示旧怪、冰谷 / 毒泽 / 冥府新怪及天界新怪，检查透明立绘与新增法宝特效的实际呈现。
- 新增三张角色图集均可通过浏览器请求返回 `200 image/png`。完整重新加载测试版后，场景素材加载完成，出发按钮可用，无素材失败提示。
- 最终测试上下文未发现 console error / warn。

开发场景注入用于边界和视觉验证，不代表真人自然通关。移动端是 Chrome 触屏模拟，未替代真实手机测试。

## 难度模拟

`npm run balance` 使用实际战斗引擎与游戏共用的 AI 策略，无气血或攻击作弊。以下是最终累加妖物池版本的固定种子样本。

| 第一境难度 | 新存档三个固定种子的结果                |
| ---------- | --------------------------------------- |
| 初入仙途   | 3 / 3 通关，310–326 秒                  |
| 问道试炼   | 3 / 3 通关，313–323 秒                  |
| 天劫降临   | 2 / 3 通关，329–332 秒；一组 139 秒失败 |

另外用 6 种新增本命法宝覆盖正道 3 组、魔道 3 组、兼修 6 组，初入仙途样本均通关；纯修只测试与流派兼容的本命。

连续六境模拟携带实际结算收益并在局间修炼炼器：前五境分别 308、389、436、497、565 秒通关。太虚天境前两次在 70、64 秒失败，第三次 612 秒通关。最终达到炼虚中期。

上述数据用于发现难度断档，并非真人胜率，也不代表所有组合均已平衡。新怪是累加到原有妖物池，原精英类型及其强度保留，后期额外加入高阶精英。

## 边界与运行注意

- 单人、本地进度，无账号或跨设备云同步。不同端口的 localStorage 相互隔离。
- 正道 / 魔道只限制新领悟；更新前的对局保留已有装备。
- 六位妖王有独立立绘，仍共用冲撞、环射与落地预警三类招式。
- AI 不保证通关，后台 / 暂停停止，结算后不自动开局。
- 本次曾因新图集未落盘就更新资源引用，触发游玩服务热更新后的素材加载失败。文件已补齐并确认可加载，随后移到独立工作树和端口开发；以后必须先确认资源到位再接入，不热更新正在游玩的实例。
